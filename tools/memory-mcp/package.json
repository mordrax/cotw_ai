{
  "name": "memory-mcp-server",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Strongly-typed MCP server for task memory — TypeScript + Bun.",
  "scripts": {
    "start": "bun run src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.5",
    "proper-lockfile": "^4.1.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/proper-lockfile": "^4.1.4",
    "typescript": "^5.7.3"
  }
}
